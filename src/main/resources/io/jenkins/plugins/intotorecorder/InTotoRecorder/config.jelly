<?jelly escape-by-default='true'?> 
<j:jelly xmlns:j="jelly:core" xmlns:f="/lib/form" xmlns:c="/lib/credentials">
    <f:entry title="${%credentialId}" field="credentialId" >
      <c:select />
    </f:entry>

    <f:entry title="${%keyPath}" field="keyPath">
      <f:textbox />
    </f:entry>

    <f:entry title="${%stepName}" field="stepName">
      <f:textbox />
    </f:entry>

    <f:entry title="${%transport}" field="transport">
      <f:textbox />
    </f:entry>
</j:jelly>

